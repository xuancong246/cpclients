/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AdPageActionBarDirective } from './ad-page-action-bar.directive';

describe('Directive: AdPageActionBar', () => {
  it('should create an instance', () => {
    let directive = new AdPageActionBarDirective();
    expect(directive).toBeTruthy();
  });
});
