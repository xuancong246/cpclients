type ColumnHeadingType = 'text' | 'languageKey';

type ColumnSortType = 'ascending' | 'descending';

type ColumnWidthType = 'distance' | 'percent' | 'unset';

type ColumnContentType = 'raw' | 'binding';

type ColumnContentAlignment = 'left' | 'center' | 'right';

type RowDropdownAction = 'delete' | 'detail' | 'edit';
